# Portfolio Optimization Project

This project performs portfolio optimization using Python.  
It loads financial data (Excel file inside `/data`) and computes optimized portfolios based on risk and return.

## Files
- `main.py` — main Python script
- `data/` — contains the Excel dataset
- `requirements.txt` — install dependencies with: `pip install -r requirements.txt`

## How to Run
